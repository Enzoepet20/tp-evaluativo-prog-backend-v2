<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrar Pago</title>
    <!-- Agregar Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Estilos personalizados */
        body {
            background-color: #f8f9fa;
        }

        .container {
            margin-top: 50px;
            max-width: 600px;
            background-color: white;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
            padding: 30px;
            border-radius: 8px;
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            font-weight: bold;
            color: #343a40;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .btn-primary {
            width: 100%;
            padding: 10px;
        }

        .alert {
            margin-bottom: 20px;
            text-align: center;
        }
        
        .card-info {
            margin-top: 20px;
        }

        .footer-text {
            text-align: center;
            margin-top: 30px;
            font-size: 14px;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Registrar Pago</h2>

        <!-- Mostrar mensajes de éxito o error -->
        <% if (typeof message !== 'undefined') { %>
            <div class="alert <%= message.includes('éxito') ? 'alert-success' : 'alert-danger' %>">
                <%= message %>
            </div>
        <% } %>

        <form action="/registrar-pago" method="POST">
            <div class="form-group">
                <label for="amount">Monto del Pago:</label>
                <input type="number" step="0.01" min="0" id="amount" name="amount" class="form-control" required>
            </div>

            <div class="form-group">
                <label for="date">Fecha del Pago:</label>
                <input type="date" id="date" name="date" class="form-control" required>
            </div>

            <div class="form-group">
                <label for="userId">Id del usuario correspondiente:</label>
                <input type="text" id="userId" name="userId" class="form-control" required>
            </div>

            <button type="submit" class="btn btn-primary">Registrar Pago</button>
        </form>
        <br>
        <br>
        <a class="btn btn-primary" aria-current="page" href="/">
            <span>Ir hacia menu principal</span>
          </a>
        <!-- Información adicional -->
        <div class="card card-info text-white bg-info mt-4">
            <div class="card-body">
                <h5 class="card-title">¿Sabías que?</h5>
                <p class="card-text">Puedes revisar el historial de pagos desde tu perfil de usuario en cualquier momento.</p>
            </div>
        </div>

        <!-- Pie de página -->
        <p class="footer-text">© 2024 Sistema de Registro de Pagos. Todos los derechos reservados.</p>
    </div>

    <!-- Scripts de Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
